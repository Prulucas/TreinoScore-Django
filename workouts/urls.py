from django.urls import path
'''from .views import dashboard, listar_treinos, detalhe_treino, editar_treino

app_name = 'workouts'

urlpatterns = [
    path('workouts', listar_treinos, name='workout'),
]
'''
